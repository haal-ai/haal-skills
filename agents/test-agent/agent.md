# test-agent

A minimal agent used to validate agent installation flow.

## Description

This agent does nothing useful. It exists solely to test that agent-type components
are correctly installed to the target repository's `.agents/` folder.
