# session-workflow

Session carry-over and carry-on workflows for Kiro.

## What this package provides

- `carry-over-session` — save session context at end of work
- `carry-on-session` — restore session context at start of work

## Usage

These are available as Kiro steering files with `inclusion: manual`.
Invoke them via the `#` context key in Kiro chat.
