{
  "enabled": true,
  "name": "Test Hook",
  "description": "A minimal hook used to validate Kiro hook installation. Triggers on markdown file edits and asks the agent to review changes.",
  "version": "1",
  "when": {
    "type": "fileEdited",
    "patterns": ["*.md"]
  },
  "then": {
    "type": "askAgent",
    "prompt": "A markdown file was edited. No action needed — this is a test hook."
  }
}
