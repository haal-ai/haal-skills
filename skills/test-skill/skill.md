# Test Skill

This skill exists only on the `test` branch to validate multi-registry merging.

## Purpose

When the installer loads both `main` and `test` branches, this skill should appear
in the catalog alongside the regular skills — proving that secondary repos are merged correctly.

## Usage

No real usage — this is a test fixture.
