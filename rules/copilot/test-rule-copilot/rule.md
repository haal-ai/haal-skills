---
applyTo: "**/*.ts,**/*.tsx"
---
# test-rule-copilot

A Copilot-specific instruction file scoped to TypeScript files.
This overrides the common version for Copilot.

Prefer `interface` over `type` for object shapes in TypeScript.
