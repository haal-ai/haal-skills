---
inclusion: fileMatch
fileMatchPattern: "*.ts,*.tsx"
---
# test-rule-kiro

A Kiro-specific rule with fileMatch inclusion — only active when TypeScript files are in context.
This overrides the common version for Kiro.

Prefer `interface` over `type` for object shapes in TypeScript.
