---
description: "TypeScript coding standards. Apply when working with .ts or .tsx files."
globs: "*.ts,*.tsx"
alwaysApply: false
---
# test-rule-cursor

A Cursor-specific rule with glob auto-attach — activates automatically for TypeScript files.
This overrides the common version for Cursor.

Prefer `interface` over `type` for object shapes in TypeScript.
